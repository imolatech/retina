<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE project>
<project name="publish" basedir=".">

    <target name="publish.locally" depends="package, ivy.publish.locally" description="Packages and publishes a snapshot to the local ivy cache." />

    <target name="publish.snapshot" depends="package, ivy.publish.snapshot" description="Packages and publishes a snapshot to Artifactory." />

    <target name="publish.milestone" depends="package, ivy.publish.milestone" description="Packages and publishes a milestone to Artifactory." />

    <target name="publish.release" depends="package, ivy.publish.release" description="Packages and publishes a release to Artifactory." />

</project>
